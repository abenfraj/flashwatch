PySide6>=6.7
mss>=9.0
rapidocr-onnxruntime>=1.3
opencv-python>=4.9
pillow>=10.0
psutil>=5.9
requests>=2.31
numpy>=1.26
pywin32>=306
keyboard>=0.13

# Only needed to package the app for someone else (see build.py); the
# application itself never imports it.
# pyinstaller>=6.0
